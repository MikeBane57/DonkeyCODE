# Copy to .env and fill in (never commit .env).
# Then: export $(grep -v '^#' .env | xargs) && npm run build
# Or on Windows PowerShell: see BUILD.md

DONKEYCODE_GITHUB_TOKEN=
DONKEYCODE_GITHUB_OWNER=
DONKEYCODE_GITHUB_REPO=
DONKEYCODE_GITHUB_BRANCH=main
DONKEYCODE_GITHUB_PATH=sessions/donkeycode-sessions.json
